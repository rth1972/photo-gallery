// AlbumGrid is superseded by the inline album card grid in albums/page.tsx.
// This shim prevents stale imports from breaking the build.
export function AlbumGrid(_props: Record<string, unknown>) {
  return null;
}
export default AlbumGrid;
